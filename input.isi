programa

  declare a, b, c, d.
  texto palavra, bota.

  leia(a).
  leia(b).
  
  bota := "ovo".
  a := 1 + 2 * 3.3 / b.
  
  se ( a > b ) entao {
     escreva (a).
  }
  senao {
     escreva ("b").
  }
  
  enquanto ( a > b) {
  	 escreva ("a").
  }

fimprog.