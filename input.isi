programa
  
  declare a, b, c, d.
  texto palavra, bota.

  # Comentarios 123 #

  leia(bota).
  leia(b).
  leia(palavra).
  leia(a).

  
  bota := "ovo".
  a := 1 + 2 * 3 / b. 
  
  # Aqui tem um comando de decisao #
  se ( a > b ) entao {
     escreva (a).
  }
  senao {
     escreva ("b").
  }
  
  enquanto ( a > b) {
  	 escreva ("a").
  }
  
  # Acabou #
fimprog.